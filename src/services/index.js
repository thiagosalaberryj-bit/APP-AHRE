/**
 * Punto de entrada para servicios externos o integraciones futuras.
 * Las funciones esenciales no deben depender de esta capa.
 */
export const serviceStatus = Object.freeze({
  connectedServicesEnabled: false,
});
